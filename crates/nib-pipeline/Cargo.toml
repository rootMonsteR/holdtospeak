[package]
name = "nib-pipeline"
version = "0.0.0"
edition = "2021"
publish = false
description = "Nib crate: nib-pipeline — the dictation orchestrator (hook → capture → ASR → inject)"

[dependencies]
nib-platform = { workspace = true }
nib-audio = { workspace = true }
nib-asr = { workspace = true }
nib-cleanup = { workspace = true }
nib-target = { workspace = true }
nib-inject = { workspace = true }
