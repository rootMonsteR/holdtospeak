{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The settings window may call the app's own commands and the core window APIs.",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
