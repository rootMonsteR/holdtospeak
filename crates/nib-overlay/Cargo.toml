[package]
name = "nib-overlay"
version = "0.0.0"
edition = "2021"
publish = false
description = "Nib crate: nib-overlay — platform-independent overlay rendering + spectrum DSP"

[dependencies]
rustfft = { workspace = true }
